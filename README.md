Projeto ER

## Setup

Opção A (recomendado): na raiz do projeto

1. `npm install` (instala automaticamente as dependências do `backend/`, incluindo o `nodemon`)
2. `npm run dev` (arranca com `nodemon`)

Opção B: diretamente no `backend/`

1. `cd backend`
2. `npm install`
3. `npm run dev`
